---
name: superpowers
description: >
  소프트웨어 개발 전체 워크플로우에 사용한다: 새 기능 개발, 버그 수정, 디버깅,
  코드 리뷰, 계획 작성, 테스트 작성, 완료 검증, 멀티에이전트 실행.
  코딩 관련 모든 작업에서 이 스킬을 먼저 확인한다.
---

# Superpowers — 소프트웨어 개발 마스터 워크플로우

## 6가지 Iron Law (절대 원칙)

```
1. 설계 승인 없이 코드 작성 금지        → brainstorming
2. 계획 없이 구현 시작 금지              → writing-plans
3. 실패 테스트 없이 구현 코드 작성 금지  → TDD
4. 근본 원인 없이 버그 수정 금지         → systematic-debugging
5. 검증 증거 없이 완료 선언 금지         → verification-before-completion
6. 리뷰 없이 태스크 완료 처리 금지       → subagent-driven-development
```

---

## 워크플로우 선택 가이드

| 상황 | 사용할 패턴 |
|------|------------|
| 새 기능/프로젝트 시작 | 브레인스토밍 → 계획 → TDD → 검증 |
| 버그/테스트 실패 | 체계적 디버깅 (4단계) |
| 독립 태스크 여러 개 | 서브에이전트 개발 |
| 완료 직전 | 완료 전 검증 |
| 코드만 수정할 때 | TDD → 검증 |

---

## 1. 브레인스토밍 (설계 우선)

<HARD-GATE>
설계 승인 없이 코드 작성, 스캐폴딩, 구현 행동 금지.
얼마나 단순해 보여도 예외 없음.
</HARD-GATE>

### 프로세스
1. **프로젝트 컨텍스트 파악** — 파일, 문서, 최근 커밋 확인
2. **명확화 질문** — 한 번에 하나씩, 목적/제약/성공 기준 파악
3. **접근법 2-3가지 제안** — 트레이드오프 + 추천 방향
4. **설계 제시** — 섹션별로 제시, 각 섹션 후 승인 확인
5. **설계 문서 저장** → `docs/plans/YYYY-MM-DD-<topic>-design.md`
6. **계획 작성으로 전환** (writing-plans)

### 핵심 원칙
- 한 번에 하나의 질문만
- YAGNI: 불필요한 기능 설계에서 제거
- 항상 2-3가지 대안 제시 후 결정

---

## 2. 계획 작성 (bite-sized 태스크)

### 태스크 단위: 2-5분짜리 단일 액션

```markdown
### Task N: [컴포넌트명]

**파일:**
- 생성: `exact/path/to/file.ts`
- 수정: `exact/path/to/existing.ts:123-145`
- 테스트: `tests/exact/path/to/test.ts`

**Step 1: 실패하는 테스트 작성**
[완전한 테스트 코드]

**Step 2: 실패 확인**
실행: `npm test -- test-name`
예상: FAIL

**Step 3: 최소 구현 코드 작성**
[완전한 구현 코드]

**Step 4: 통과 확인**
실행: `npm test -- test-name`
예상: PASS

**Step 5: 커밋**
```bash
git add [files]
git commit -m "feat: ..."
```
```

### 계획 헤더 필수 포함
```markdown
# [기능명] 구현 계획
**목표:** [한 문장]
**아키텍처:** [2-3문장]
**기술 스택:** [핵심 기술]
```

### 저장 위치
`docs/plans/YYYY-MM-DD-<feature-name>.md`

---

## 3. TDD (테스트 주도 개발)

### Iron Law
```
실패하는 테스트를 먼저 작성한다.
실제로 실패하는 것을 눈으로 확인한다.
통과시킬 최소한의 코드를 작성한다.
```

### RED → GREEN → REFACTOR

**RED**: 실패하는 테스트 작성
```typescript
// 아직 존재하지 않는 함수를 테스트
test('calculateTotal returns sum of items', () => {
  expect(calculateTotal([10, 20, 30])).toBe(60);
});
// 실행 → FAIL (함수 없음)
```

**GREEN**: 통과시킬 최소 코드
```typescript
function calculateTotal(items: number[]): number {
  return items.reduce((sum, item) => sum + item, 0);
}
// 실행 → PASS
```

**REFACTOR**: 동작 유지하며 코드 개선
```typescript
// 중복 제거, 명확성 향상, 성능 최적화
// 매 변경 후 테스트 재실행
```

### 안티패턴

| 변명 | 현실 |
|------|------|
| "간단해서 테스트 불필요" | 단순한 변경이 복잡한 버그를 만든다 |
| "나중에 테스트 작성" | 나중은 오지 않는다 |
| "수동 검증으로 충분" | 수동 검증은 회귀를 막지 못한다 |

---

## 4. 체계적 디버깅

### Iron Law
```
근본 원인 파악 없이 수정 금지.
증상을 고치는 것은 실패다.
```

### 4단계 프로세스

**Phase 1: 근본 원인 조사** (수정 전 필수)
1. 에러 메시지 완전히 읽기 (스킵 금지)
2. 재현 가능한지 확인 — 재현 불가 시 데이터 더 수집
3. 최근 변경 사항 확인 — `git diff`, 최근 커밋
4. 멀티 컴포넌트 시스템: 각 레이어에 진단 로그 추가 후 실행

**Phase 2: 패턴 분석**
- 동작하는 유사 코드 찾기
- 동작 vs 미동작 차이 목록 작성

**Phase 3: 가설 & 테스트**
- 가설 명확히 서술: "X가 원인인 이유는 Y이기 때문"
- 최소한의 변경으로 가설 검증 (한 번에 하나씩)

**Phase 4: 구현**
- 실패하는 테스트 케이스 먼저 작성
- 근본 원인 수정 (증상 아님)
- 수정 후 검증

### 3회 수정 실패 시 규칙
3번 이상 수정해도 해결 안 되면 → 아키텍처 문제 의심 → 사용자와 논의

### Red Flags (즉시 중단)
- "빠른 수정 후 나중에 조사"
- "X를 바꿔보면 될 것 같아"
- "원인은 모르지만 이게 맞을 것 같아"
- 데이터 흐름 추적 없이 해결책 제안

---

## 5. 완료 전 검증

### Iron Law
```
검증 명령을 실행하지 않았으면 완료라고 말할 수 없다.
```

### Gate Function (완료 선언 전 필수)
```
1. 무엇을 실행해야 이 주장이 증명되는가?
2. 그 명령을 지금 실행한다
3. 전체 출력을 읽는다
4. 출력이 주장을 확인하는가?
   - NO → 실제 상태를 증거와 함께 보고
   - YES → 증거와 함께 주장
```

### 검증 실패 패턴

| 주장 | 필요한 것 | 불충분한 것 |
|------|----------|------------|
| 테스트 통과 | 테스트 실행 결과: 0 실패 | "아마 통과할 것" |
| 빌드 성공 | 빌드 명령: exit 0 | 린터 통과 |
| 버그 수정 | 원래 증상 테스트: 통과 | 코드 변경됨 |
| 요구사항 충족 | 항목별 체크리스트 | 테스트 통과 |

### Red Flags
- "should", "probably", "아마", "될 것 같아" 사용
- 검증 전 만족감 표현 ("완료!", "됐어요!")
- 검증 없이 커밋/PR 생성

---

## 6. 서브에이전트 개발

**사용 시점**: 구현 계획이 있고 독립적인 태스크 여러 개를 현재 세션에서 실행할 때.

### 프로세스
```
계획 읽기 → 전체 태스크 추출 → TodoWrite 생성
  ↓ 태스크별 반복:
구현 서브에이전트 디스패치
  → 사양 준수 리뷰 (spec compliance)
  → 코드 품질 리뷰 (code quality)
  → 둘 다 ✅ → 태스크 완료
  ↓
최종 리뷰 → 브랜치 마무리
```

### 2단계 리뷰 (순서 중요)
1. **사양 준수 리뷰**: 계획대로 구현됐는가? 누락/추가 없는가?
2. **코드 품질 리뷰**: 코드가 잘 작성됐는가?

**사양 리뷰 통과 전 품질 리뷰 시작 금지.**

### Red Flags
- 여러 구현 서브에이전트 병렬 실행 (충돌)
- 리뷰 건너뜀
- 이슈가 있는데 다음 태스크로 진행

---

## 공통 안티패턴

| 변명 | 현실 |
|------|------|
| "간단해서 설계 불필요" | 단순한 프로젝트에서 미검토 가정이 가장 많은 낭비를 만든다 |
| "급해서 프로세스 생략" | 체계적 접근이 추측보다 빠르다 |
| "원인은 모르지만 이게 맞을 것" | 이해 없는 수정 = 새 버그 생성 |
| "다음에 리팩토링" | 나중은 오지 않는다 |
| "에이전트가 성공했다고 했어" | 독립적으로 검증하라 |
